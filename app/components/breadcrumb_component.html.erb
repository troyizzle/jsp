<div class="flex flex-row flex-wrap space-x-2">
  <% items.each do |item| %>
    <%= link_to item[:name], item[:url], class: "text-sm text-gray-600 hover:text-gray-800" %>
    
    <% unless last_item?(item) %>
      <span class="text-sm text-gray-600">></span>
    <% end %>
  <% end %>
</div>
